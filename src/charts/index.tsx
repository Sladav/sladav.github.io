
import * as React from 'react';
import * as ReactDOM from 'react-dom/client';
import { DiceRollChart } from './startChart';

const root = ReactDOM.createRoot(document.getElementById('root')!)

root.render(
    <div>
        <DiceRollChart/>
    </div>
    
)